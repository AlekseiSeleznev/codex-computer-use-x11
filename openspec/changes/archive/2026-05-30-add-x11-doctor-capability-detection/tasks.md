## 1. Apply Preconditions and Task/Evidence Setup

- [x] 1.1 Re-confirm the apply baseline before code edits: `git status --short` is clean, the already-verified installed-CLI command `openspec status --change add-x11-doctor-capability-detection --json` shows all planning artifacts complete, and `openspec validate add-x11-doctor-capability-detection --type change --json` succeeds as the canonical installed OpenSpec CLI validation command. If a future OpenSpec version removes the `openspec status --change ... --json` form, inspect the change directory manually for all planning artifact files and proceed only when the canonical validate command succeeds.
- [x] 1.2 Create or update the apply evidence notes in `test-plan.md` before marking any implementation slice complete; each RED/GREEN/refactor entry must include exact command, exit status, and a short output excerpt or log path.
- [x] 1.3 Record implementation anchors before coding: `doctor-capability-detection` is claimed in `capabilities.implemented` at Slice 1 when the expanded additive report skeleton exists; `x11-ewmh-windowing` remains in `capabilities.planned`; no separate ydotoold process-hint/`pgrep` field is planned unless a later task explicitly opts into it.
- [x] 1.4 Record exact Slice 4 EWMH fixture strings for evidence: valid supporting-WM fixture uses `_NET_SUPPORTING_WM_CHECK(WINDOW): window id # 0x1234abcd`; zero-id failure fixture uses `_NET_SUPPORTING_WM_CHECK(WINDOW): window id # 0x0`; absent-property fixture omits `_NET_SUPPORTING_WM_CHECK` entirely while including `_NET_ACTIVE_WINDOW(WINDOW): window id # 0x1234abcd`; non-zero-exit fixture records the command failure separately. Slice 4 GREEN evidence is formally blocked until this fixture format is verified by at least one of: a live `xprop -root _NET_SUPPORTING_WM_CHECK _NET_ACTIVE_WINDOW` run, matching output already present in repository evidence, or an authoritative xprop documentation citation. Do not defer this confirmation to Slice 11 live smoke; fixture-first verification is required before Slice 4 GREEN is marked complete.
- [x] 1.5 Plan the Slice 9 CLI seam before implementation: keep `main.rs` thin and route command execution through a testable handler that accepts a report builder plus stdout/stderr `Write` outputs so report-builder or pre-write output failures can be simulated without partial JSON.
- [x] 1.6 Record draft Slice 8 AT-SPI blocker string anchors before coding: `AT-SPI bus unreachable` and `AT-SPI bus reachable but tree extraction unavailable`; final chosen strings may differ, but they must remain textually distinct and be recorded in the Slice 8 evidence row before completion.
- [x] 1.7 Record the privacy carve-out rule before coding: keep private DBus session-bus addresses and environment-derived private socket paths out of serialized JSON, while allowing well-known ydotool socket candidate paths required by Slice 5 diagnostics.

## 2. Additive Report Skeleton and Checks Contract

- [x] 2.1 RED Slice 1: add `doctor_cli_expanded_report_preserves_bootstrap_contract` as one contract-preservation integration test asserting existing bootstrap fields stay type-stable while new readiness fields and top-level fact sections exist.
- [x] 2.2 GREEN Slice 1: introduce additive serializable report structs and `doctor::report_from_system()`/equivalent wrapper that emits deterministic unavailable/default facts, includes `doctor-json` and `doctor-capability-detection`, and preserves `x11-ewmh-windowing` in `capabilities.planned`.
- [x] 2.3 RED Slice 2: add `doctor_cli_checks_are_truthful_for_capability_stage` asserting `checks[*].name/ok/detail`, no stale `no-live-x11-probes` detail, truthful read-only probe-policy checks, and one doctor-internal self-check.
- [x] 2.4 GREEN Slice 2: replace the stale bootstrap check with truthful checks; record the final chosen `checks[]` names in `test-plan.md` evidence before marking Slice 2 complete.

## 3. Environment, Tool, and EWMH Probe Facts

- [x] 3.1 RED Slice 3: add `doctor_env_classifies_cinnamon_x11`, `doctor_no_display_returns_structured_report`, and `doctor_env_does_not_serialize_dbus_addresses_or_socket_paths` using fake env maps.
- [x] 3.2 GREEN Slice 3: add `ProbeContext`/environment snapshot seams and privacy-preserving environment facts; no raw DBus session bus addresses, environment-derived private socket paths, private URLs, or `.secrets.local.env` values may be serialized. This privacy rule must not suppress explicit ydotool socket candidate path details required by Slice 5 because those candidates are well-known public filesystem paths or user-provided diagnostic candidates, not private DBus session-bus values.
- [x] 3.3 RED Slice 4: add `doctor_tools_report_available_and_missing_commands`, `doctor_can_query_windows_requires_x11_tools_and_xprop_probe`, and `doctor_can_query_windows_false_for_missing_display_tool_or_absent_ewmh` with fake `command -v` and `xprop -root _NET_SUPPORTING_WM_CHECK _NET_ACTIVE_WINDOW` outputs.
- [x] 3.4 GREEN Slice 4: complete Task 1.4 fixture-format verification first, then add command-runner seam, full tool report for `busctl`, `gdbus`, `wmctrl`, `xprop`, `xdotool`, and `ydotool`, canonical read-only EWMH parser, non-zero supporting-WM id success, and discrete failures for missing `DISPLAY`, missing `wmctrl`/`xprop`, non-zero `xprop` exit, zero/null supporting-WM id, and absent requested property; then wire `can_query_windows` aggregation input.

## 4. Ydotool, Portal/Screenshot, Accessibility, and Input Facts

- [x] 4.1 RED Slice 5: add `doctor_ydotool_socket_continues_after_stale_candidates`, `doctor_ydotool_socket_falls_back_when_env_candidate_absent`, and `doctor_ydotool_socket_reports_no_connectable_socket` using fake env and socket connector.
- [x] 4.2 GREEN Slice 5: implement ydotool candidate ordering `YDOTOOL_SOCKET`, `$XDG_RUNTIME_DIR/.ydotool_socket`, `/tmp/.ydotool_socket`, skip unset/empty env candidate, continue past stale paths, select first connectable socket, and avoid `pgrep` unless a process-hint field is explicitly added with `doctor_ydotool_process_hint_uses_optional_pgrep_without_blocking` evidence.
- [x] 4.3 Slice 6 prerequisite: before writing RED tests, record in the Slice 6 evidence row the provisional candidate RemoteDesktop method/property names the positive fixture will use, including whether they are the design-suggested `CreateSession`, `SelectDevices`, `Start`, `NotifyPointer*`/`NotifyKeyboard*` names or documented equivalents; reconcile these provisional names against the actual positive fixture used when Task 4.5 GREEN evidence records final confirmed names.
- [x] 4.4 RED Slice 6: add parser fixture tests `portal_parser_rejects_empty_remote_desktop_table`, `portal_parser_accepts_remote_desktop_with_required_methods`, `portal_parser_accepts_screenshot_v2_method`, `screenshot_parser_accepts_cinnamon_gnome_shell_methods`, and `portal_report_marks_screencast_input_capture_report_only`; the report-only `screencast`/`input_capture` test must assert shape presence and also assert these facts do not alter `readiness.ok` and do not add blockers.
- [x] 4.5 GREEN Slice 6: implement pure DBus/portal/screenshot parsers with fixtures for empty tables, `CreateSession`, `SelectDevices`, `Start`, `NotifyPointer*`/`NotifyKeyboard*` or documented equivalent, Screenshot v2 `Screenshot`, Cinnamon `org.gnome.Shell.Screenshot`, and report-only `screencast`/`input_capture`; always record final accepted RemoteDesktop method/property names in the `test-plan.md` Slice 6 evidence row before marking the slice complete, whether they match the suggested names or use documented equivalents.
- [x] 4.6 RED Slice 7: add `doctor_accessibility_distinguishes_bus_from_enabled_state` and `doctor_input_backends_derive_abs_pointer_locally` with fake command/file probes.
- [x] 4.7 GREEN Slice 7: implement accessibility facts, `/dev/uinput` read/write file-access seam, local `abs_pointer` derivation, ydotool/portal input facts, and xdotool candidate-only fact without calling or embedding target repo `CapabilityMap`.

## 5. Readiness Aggregation and CLI Behavior

- [x] 5.1 RED Slice 8: add readiness aggregation tests `doctor_readiness_ok_is_false_when_blockers_exist`, `doctor_degraded_reasons_are_separate_from_blockers`, `doctor_development_input_is_or_of_supported_backends`, `doctor_focus_booleans_remain_false_until_verified_focus_design`, `doctor_readiness_ok_when_all_conditions_met`, and `doctor_recommended_next_step_priority_is_deterministic`; explicitly assert `readiness.can_focus_apps == false` and `readiness.can_focus_windows == false` as stable for this entire change until a later accepted design/spec changes focus behavior; recommendation-priority fixtures may be implemented as parameterized/table-driven tests rather than separate functions for every priority tier.
- [x] 5.2 GREEN Slice 8: implement blocker-based readiness aggregation from facts only; keep `can_focus_apps` and `can_focus_windows` false; make `can_send_development_input` true only for local `abs_pointer`, connectable ydotool socket, or strict RemoteDesktop; record final distinct AT-SPI blocker strings for unreachable bus vs reachable-bus/tree-unavailable fixtures. If implementation starts treating the readiness vocabulary as a cross-consumer contract beyond additive `doctor --json` behavior, stop and reopen the ADR gate before continuing.
- [x] 5.3 RED Slice 9: add CLI process tests `doctor_cli_success_emits_single_json_object`, `doctor_cli_no_display_emits_single_json_object`, `doctor_cli_rejects_unsupported_usage`, and `doctor_cli_internal_failure_reports_stderr_without_partial_json` through the planned handler seam. No TDD exception is pre-approved; if the seam proves impractical, stop and seek an explicit `test-plan.md` exception update before weakening or skipping the serialization-failure/no-partial-JSON test.
- [x] 5.4 GREEN Slice 9: refactor CLI handling to return correct exit categories, emit exactly one JSON object on success/no-display, keep unsupported usage non-zero with stderr, and ensure simulated internal/report/write failure is non-zero with no partial JSON.

## 6. Source Overlay Metadata and Live Smoke

- [x] 6.1 RED Slice 10: add `doctor_source_overlay_is_static_acceptance_metadata` and `doctor_does_not_modify_configured_target_checkout` using a fake target path/temp directory.
- [x] 6.2 GREEN Slice 10: add static `source_overlay` metadata with `strict_portal_required`, target vocabulary names (`PortalReport`, `InputReport`, `WindowingReport`, `ReadinessReport`), screenshot provider mapping labels, `target_checkout_modified == false`, and no writes to `CODEX_DESKTOP_LINUX_FULL_PATH` or the documented local default target checkout path.
- [x] 6.3 External-code guard: keep `joe223/sootie` and other refreshed repositories ideas-only; do not copy/adapt external code unless a later artifact/task resolves license and attribution explicitly.
- [x] 6.4 Slice 11 eligibility and RED setup: run this slice only after Slices 1-10 are GREEN; then collect RED evidence if the live jq smoke command fails against already-GREEN unit/fixture expectations, and never use live desktop output to define behavior before unit/fixture tests exist.
- [x] 6.5 Complete Slice 11 live smoke: run `cargo run -- doctor --json | jq -e '.backend == "x11-ewmh" and (.checks|type=="array") and (.readiness.degraded_reasons|type=="array") and (.readiness.can_query_windows|type=="boolean") and (.readiness.can_focus_apps|type=="boolean") and (.readiness.can_focus_windows|type=="boolean") and (.readiness.can_send_development_input|type=="boolean")'`; run no-display smoke with `env -u DISPLAY cargo run -- doctor --json | jq -e '.readiness.can_query_windows == false'` when shell-level env manipulation is reliable, otherwise record Slice 3/Slice 9 fixture fallback evidence.

## 7. Required Verification, Spec Follow-ups, and Archive Readiness

- [x] 7.1 Run `make fmt`, `make check`, and `make test`; fix failures before marking implementation complete.
- [x] 7.2 Run the canonical OpenSpec validation command first in apply-complete evidence: `openspec validate add-x11-doctor-capability-detection --type change --json`; treat failure or missing command as a blocker.
- [x] 7.3 Verify `git status --short` does not include `.secrets.local.env`, `.codex/session/`, unintended target-checkout paths, or unrelated dirty work.
- [x] 7.4 Ensure `test-plan.md` evidence log is complete for every slice, including canonical EWMH fixture strings from Task 1.4 in the Slice 4 evidence row, final `checks[]` names for Slice 2, final RemoteDesktop method/property names for Slice 6, final distinct AT-SPI blocker strings for Slice 8, conditional Slice 5 `doctor_ydotool_process_hint_uses_optional_pgrep_without_blocking` evidence if a process-hint field was added, and any explicit approved exception if one was required.
- [x] 7.5 Re-run the Task 6.5 live `doctor --json` smoke and no-display smoke as archive-final evidence before verification/archive; if live shell-level env manipulation is unavailable, explicitly reference the Slice 3/Slice 9 fixture fallback evidence and explain why the live no-display command could not be repeated.
- [x] 7.6 Record the canonical spec `Purpose: TBD` maintenance debt for both `openspec/specs/doctor-cli/spec.md` and `openspec/specs/x11-integration-contract/spec.md` as an archive/spec-sync follow-up; file it after this change archives as a dedicated spec-maintenance/backlog change rather than expanding this behavior change, and do not let it disappear silently.
- [x] 7.7 Confirm `capabilities.planned` handling is explicit: `x11-ewmh-windowing` remains planned until a later listing/focus change implements it or moves it into named capability facts.
- [x] 7.8 Run the canonical verification workflow for `add-x11-doctor-capability-detection` by applying the project-local `openspec-verify-change` skill (equivalent user prompt: `/opsx:verify add-x11-doctor-capability-detection`); at minimum, produce a verification report over completed tasks, specs, design/design-review/ADR/test-plan evidence, and resolve any CRITICAL issues before archive.
- [x] 7.9 Archive only after verification is clean/accepted, all tasks are checked, specs sync assessment is complete, the working tree is clean except intended archive/spec-sync changes, and the previously granted user authorization for archive still applies without any new hard-gate conflict.
